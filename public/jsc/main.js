(function() {
  require(["jquery", "underscore", "jquery.fittext", "jquery.autoresize", "jquery.autogrow", "jquery.tags", "jquery.chosen"], function($) {
    return $(function() {
      return $("body").alpha().beta();
    });
  });

}).call(this);
